{
  "name": "remotescreen-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Simple RemoteScreen MVP signaling server and Host page",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
